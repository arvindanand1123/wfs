[project]
name = "wfs"
version = "0.1.0"
description = "A generic S3-compatible storage gateway (FastAPI) deployed on Cloudflare Containers."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "boto3>=1.34",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "httpx>=0.27",
    "ruff>=0.5",
    "moto[server]>=5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wfs"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q"

[tool.ruff]
line-length = 200
target-version = "py311"
src = ["src", "tests"]
# ruff >=0.16 formats Python code blocks embedded in Markdown by default.
# AGENTS.md's examples are hand-aligned on purpose (including deliberately
# non-conforming "bad" examples), so keep the formatter out of the docs.
extend-exclude = ["*.md"]

[tool.ruff.lint]
# E/W pycodestyle, F pyflakes, I isort, UP pyupgrade, B bugbear, SIM simplify
select = ["E", "W", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.isort]
known-first-party = ["wfs"]
